import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.setsandrepsapp',
  appName: 'Sets & Reps',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
