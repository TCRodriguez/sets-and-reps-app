import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sets-and-reps-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
